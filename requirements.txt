# Runtime dependencies (what the app imports + needs to serve).
# Curated to direct deps; pip resolves the transitive ones on install.

# Web / API
fastapi==0.136.1
uvicorn==0.46.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ML / NLP
torch==2.12.0
transformers==5.8.1
peft==0.19.1
sentencepiece==0.2.1
sentence-transformers==5.5.1
scikit-learn==1.8.0
nltk==3.9.4

# Source retrieval / content extraction
trafilatura==2.0.0
ddgs==9.14.4

sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0